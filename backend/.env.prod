# 운영용 환경변수 설정
# ======================
# 주의: 실제 운영에서는 이 파일을 사용하지 말고 시스템 환경변수 사용!

# 데이터베이스 설정 (운영용 - 반드시 변경 필요!)
DB_HOST=your-production-db-host.com
DB_PORT=5432
DB_NAME=octave_prod
DB_USER=octave_user
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD

# API 설정 (운영 서버)
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=false

# CORS 설정 (운영용 - 실제 도메인만 허용)
FRONTEND_URL=https://your-frontend-domain.com

# 보안 설정 (운영용 - 강력한 키 필수!)
SECRET_KEY=CHANGE_THIS_TO_VERY_STRONG_SECRET_KEY_256_BITS
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 파일 업로드 설정 (운영용)
MAX_FILE_SIZE_MB=50
ALLOWED_AUDIO_FORMATS=audio/mpeg,audio/wav,audio/mp4,audio/aac,audio/m4a,audio/webm

# 음성 분석 설정 (운영용)
PITCH_THRESHOLD=0.1
SAMPLE_RATE=22050
CONFIDENCE_MIN_THRESHOLD=0.3

# FFmpeg 설정 (운영용 - 절대 경로 권장)
FFMPEG_PATH=/usr/bin/ffmpeg

# 로깅 설정 (운영용 - 에러만)
LOG_LEVEL=ERROR

# 운영 전용 플래그
DEBUG=false
ENABLE_DOCS=false
